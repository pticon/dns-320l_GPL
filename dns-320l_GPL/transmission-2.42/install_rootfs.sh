#!/bin/sh

#${CROSS_COMPILE}strip -v daemon/transmission-daemon -o ${ROOT_FS}/usr/bin/transmission-daemon
#${CROSS_COMPILE}strip -v daemon/transmission-remote -o ${ROOT_FS}/usr/bin/transmission-remote

#cp -rf ${TOOLCHAIN_FS}/share/transmission/web 
